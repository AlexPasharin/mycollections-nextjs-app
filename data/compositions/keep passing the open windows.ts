import type { Composition } from "types/discography";

const data: Composition = {
  name: `Keep Passing The Open Windows`,
  artist: `Queen`,
  versions: [
    {
      id: `kptow`,
    },
    { id: `kptow-ext`, versionName: "extended version" },
  ],
};

export default data;
