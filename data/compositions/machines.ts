import type { Composition } from "types/discography";

const data: Composition = {
  name: `Machines (Or 'Back To Humans')`,
  artist: `Queen`,
  versions: [
    {
      id: `machines`,
    },
    { id: `machines-instru`, versionName: "instrumental remix" },
  ],
};

export default data;
